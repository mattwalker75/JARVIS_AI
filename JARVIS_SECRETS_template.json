{
  "_comment": "Credentials for the USER'S OWN accounts that JARVIS may log into on your behalf. Copy to JARVIS_SECRETS.json (gitignored) and fill in. JARVIS can read these (list_secrets shows names only; get_secret returns the password) to log in to sites you already own. Do NOT use this to have JARVIS create new accounts.",
  "secrets": {
    "example-email": {
      "username": "you@example.com",
      "password": "REPLACE_ME",
      "url": "https://mail.example.com",
      "notes": "Personal email account"
    }
  }
}
